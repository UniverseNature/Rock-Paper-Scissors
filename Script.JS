function myFunction(playersMove){
    playersMove = prompt("Rock, Paper or Scissors ?")
    var min= 1; 
	var max= 3; 
    var random = Math.random() * (+max - +min) + +min;  
    var enemyMove;
    if(random===1){enemyMove}
}